{
  "name": "droneportfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:images": "node scripts/prepare-images.mjs",
    "build": "node scripts/build.mjs",
    "check": "node --test && npm run build",
    "serve": "python3 -m http.server 4173 --bind 127.0.0.1 --directory dist"
  },
  "dependencies": {
    "sharp": "^0.35.4"
  },
  "engines": {
    "node": ">=20"
  }
}
